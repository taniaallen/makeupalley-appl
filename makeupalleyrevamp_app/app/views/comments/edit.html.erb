<h1>Edit Your Comment</h1>

<%= form_for([@topic, @comment]) do |f| %>
	<%= f.text_area :comment %>
	<%= f.submit 'Update Comment' %>
<% end %>

<%= link_to 'Cancel and return to topic', topic_path(@topic) %>